# PHP-FPM Queue Monitor Configuration
# This file is sourced by the systemd service

# Monitoring interval in seconds
INTERVAL=10

# CloudWatch namespace for metrics
NAMESPACE=PhpFpm

# AWS Region
AWS_REGION=us-east-1

# Additional command line arguments (including dimensions)
# Add dimensions here: --dimension key1=value1 --dimension key2=value2
EXTRA_ARGS=""
